# safelist
branches:
  only:
  - master
  - testing
  - develop

# blacklist
branches:
  except:
  - mvn-repo

language: java
jdk: oraclejdk8

env:
  - MAVEN_VERSION=3.3.9

script:
  - ./mvn clean install

cache:
  directories:
  - $HOME/.m2